const vec3 wind_direction = vec3(1.0, 0.0, 0.0);

const float lower_clouds_height     = 1600.0;
const float lower_clouds_thickness  = 1000.0;

const float lower_clouds_coverage   = 0.375;
const float lower_clouds_anvil_bias = 1.0;

const vec3  lower_clouds_scattering_top     = vec3(0.12, 0.112152, 0.096384) * 0.67;
const vec3  lower_clouds_scattering_bottom  = vec3(0.12, 0.112152, 0.096384) * 1.49;
const float lower_clouds_scattering_remap   = 0.125;

const float silver_intensity = 1.0;
const float silver_spread = 1.14;